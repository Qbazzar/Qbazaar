# QBazaar — خطة إصلاحات التصميم والريسبونسيف

تدقيق شامل لكل صفحات الموقع (عام + حساب/مصادقة) على الموبايل (~375px) والتابلت (~768px) + جودة التصميم. مرتّبة بالأولوية. معظم الإصلاحات في `qbazaar-web/styles/qbfront.css` (CSS) أو أصناف Tailwind بالمكوّنات.

النمط الجذري المتكرر: **breakpoints الـCSS الحالية (1100/900/760/540px) لا تحاذي مقاسات التابلت الفعلية (768px)** → شبكات كثيرة الأعمدة تنضغط في نطاق 760–1100px. الحل العام: إضافة نقطة 768px.

---

## المستوى 1 — أولوية عالية (تؤثر على كل الصفحات / تكسر الاستخدام)

| # | المكان | المشكلة | الإصلاح |
|---|--------|---------|---------|
| 1 | `qbfront.css` `.cat-listings` / `.subcat-grid` (899) | شبكة الإعلانات تبقى 3 أعمدة من 1100→540px → كروت ~248px على التابلت (تظهر بكل صفحات الإعلانات) | 3 أعمدة ≥1100، **2 عند 900**، 1 عند 540 |
| 2 | `qbfront.css` `.cat-grid` (625) | شبكة الفئات 4 أعمدة حتى 1100px → ضغط شديد على التابلت | 4≥1100 · 3 عند 1024 · **2 عند 768** · 1 عند 540 |
| 3 | `qbfront.css` `.category-grid` (1664) | breakpoint غريب: تبقى عمودين من 760→900 | محاذاة لـ768/1024/1240 |
| 4 | `qbfront.css` `.search-bar` (594) | 5 أعمدة تنهار فجأة لعمودين عند 900 ثم 1 عند 760 | إضافة 3 أعمدة وسيطة عند ~900 |
| 5 | `qbfront.css` `.cat-pick-grid` (1062) — ويزرد النشر | منتقي الفئة `minmax(160px)` → عمودين ~80px على الموبايل (غير صالح) | عمود واحد <480px |
| 6 | **نماذج الحساب** (`ProfileForm`, `PasswordChangeForm`) | بعد توسعة الحساب لـ1600px، حقول الإدخال تتمدّد ~1400px على الديسكتوب | لفّ محتوى النماذج بـ`max-w-xl`/`max-w-2xl` داخلياً |
| 7 | `qbfront.css` `.detail-grid` sidebar mobile | عند انهيار الشبكة، الـ380px sidebar يصير 100% (احتمال overflow) + كان sticky يغطّي المحتوى | (sticky أُصلح ✓) — التأكد من إزالة sticky/الثبات على الموبايل |

---

## المستوى 2 — أولوية متوسطة

| # | المكان | المشكلة | الإصلاح |
|---|--------|---------|---------|
| 8 | `RegisterForm.tsx:145` | كروت نوع الحساب `grid-cols-2` تنضغط على الموبايل | `grid-cols-1 sm:grid-cols-2` |
| 9 | صفحات المصادقة (login/register/forgot) | عناوين بأحجام px ثابتة inline بلا تدرّج | `font-display text-3xl sm:text-4xl` |
| 10 | `qbfront.css` `.profile-head` (1313) | رأس الملف العام: الأزرار تبقى inline مضغوطة على الموبايل | `flex-col` + عرض كامل للأزرار <480px |
| 11 | `qbfront.css` `.stats-grid` (1340) | إحصاءات الملف تبقى عمودين حتى 540 → ~180px مضغوط | عمود واحد <540 |
| 12 | `verification/page.tsx` صفّ التحقّق | أيقونة+نص+زر في صفّ واحد يتزاحم على الموبايل | `flex-col sm:flex-row` |
| 13 | `qbfront.css` `.topic-grid` (1480) help | `minmax(220px)` صارم → عمود واحد مبكراً عند 540 | `minmax(160px)` أو breakpoints صريحة |
| 14 | `AdGallery.tsx` شريط المصغّرات | overflow أفقي + سحب مزدوج على الموبايل | حدّ المصغّرات + flex-basis |
| 15 | `sessions/page.tsx:34` | حشوة بطاقة ضيقة جداً `p-2 sm:p-3` | `p-4 sm:p-5` |
| 16 | `city-tags` الهوم (698) | دورانات النص تتداخل/تختفي على الموبايل | `transform:none` <480px |

---

## المستوى 3 — تلميع (أولوية منخفضة)

| # | المكان | المشكلة | الإصلاح |
|---|--------|---------|---------|
| 17 | اتساق المسافات | نماذج المصادقة `space-y-4` مقابل الحساب `space-y-6` | توحيد `space-y-5/6` |
| 18 | اتساق البطاقات | خلط `ring-1` مع border + حشوات مختلفة | توحيد `ring-1 ring-foreground/10 rounded-2xl` |
| 19 | `.empty-state` (1398) | حالات فارغة تبدو غير مكتملة | حشوة + حدود + توسيط واضح |
| 20 | `.filter-group` (819) | فواصل صفوف كثيرة تبدو generic | left-accent بدل border-bottom |
| 21 | `.cat-card__icon` (639) / دعم/CMS | أيقونات/عرض نص صغيرة الحجم نسبياً | تكبير + اتساق العرض الأقصى |

---

## التنفيذ المقترح
- **دفعة A (المستوى 1):** تصليب breakpoints (بنود 1–6) — أعلى أثر، CSS غالباً + max-width للنماذج. أنصح بالبدء بها.
- **دفعة B (المستوى 2):** مكوّنات محدّدة (8–16).
- **دفعة C (المستوى 3):** تلميع بصري (17–21).

كل دفعة: تعديل → typecheck → نشر → تتأكد بصرياً.
