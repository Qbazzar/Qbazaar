{
  "name": "qbazaar-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "OpenAPI spec, error codes, roadmap and planning docs for QBazaar.",
  "scripts": {
    "mock": "prism mock openapi/v1.yaml --port 4010 --host 0.0.0.0",
    "proxy": "prism proxy openapi/v1.yaml http://localhost:8000 --port 4010 --errors",
    "validate": "redocly lint openapi/v1.yaml"
  },
  "devDependencies": {
    "@stoplight/prism-cli": "^5.10.0",
    "@redocly/cli": "^1.25.0"
  }
}
